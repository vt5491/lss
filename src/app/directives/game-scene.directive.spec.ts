import { GameSceneDirective } from './game-scene.directive';

describe('GameSceneDirective', () => {
  it('should create an instance', () => {
    const directive = new GameSceneDirective();
    expect(directive).toBeTruthy();
  });
});
